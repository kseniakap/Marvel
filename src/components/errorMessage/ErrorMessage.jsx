import React from 'react'
import error from "./error.gif"
const ErrorMessage = () => {
  return (
     <img src={error} alt="error" style={{display:"block", margin:"0 auto"}} />
   )
}

export default ErrorMessage
